<form method="post" action="{{ path('pismo_delete', {'id': pismo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pismo.id) }}">
    <button class="btn">Delete</button>
</form>
