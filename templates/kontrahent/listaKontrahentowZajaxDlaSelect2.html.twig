{
    "results": [
        {% for kontr in kontrahents %}
            {
                "id": "{{ kontr.id }}",
                "text": "{{ kontr.nazwa }}"
            }{{ not loop.last ? ', ' }}
        {% endfor %}
    ]
}


    


