{
    "results": [
        {% for poz in pozycje %}
            {
                "id": "{{ poz.id }}",
                "text": "{{ poz.nazwa }}"
            }{{ not loop.last ? ', ' }}
        {% endfor %}
    ]
}


    


